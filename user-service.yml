server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/user_db
    username: root
    password: test

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

jwt:
  algorithm:
    key: ibms-secret-key-ibms-secret-key
  issuer: ibms-app
  expiry:
    duration: 3600000
